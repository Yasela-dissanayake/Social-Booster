# Local Development Environment Variables
# Copy to .env if you want to customize these values

# Node Environment
NODE_ENV=development

# Database (automatically provided by Replit)
# DATABASE_URL=postgresql://... (already set in environment)

# Session Security (automatically generated)
# SESSION_SECRET=... (already set in environment)

# AI Services
# OPENAI_API_KEY=... (already set, but your custom AI engine works without it)

# Optional Services (not required for local development)
# STRIPE_SECRET_KEY=sk-test-... (for payment features)
# STRIPE_PUBLISHABLE_KEY=pk-test-... (for frontend payments)

# App Configuration
PORT=5000
VITE_API_BASE_URL=http://localhost:5000

# Feature Flags for Local Development
CUSTOM_AI_ENABLED=true
OPENAI_FALLBACK_ENABLED=false
STRIPE_ENABLED=false